{{> nav }}
{{> slider }}
{{> indexsection }}
<div class="container">
        <div class="col s12">
            <h3>Index</h3>
        </div>
        
</div>

<script>
      $(document).ready(function () {
      // Init Side nav
      $('.button-collapse').sideNav();

      // Init Slider
      $('.slider').slider({
        indicators: false,
        height: 500,
        transition: 500,
        interval: 6000
      });

    });
</script>